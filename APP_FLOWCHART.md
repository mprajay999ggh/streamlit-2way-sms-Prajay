# Detailed Flowchart: 2-Way SMS Case Management Application

## 🎯 Application Overview
This Streamlit application manages inbound SMS cases, allowing care coordinators to review member messages, send templated responses, and close cases with proper documentation.

---

## 📊 Main Application Flow

```
┌─────────────────────────────────────────────────────────────────┐
│                      APPLICATION START                           │
└─────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────┐
│                   AUTHENTICATION LAYER                           │
│  ┌────────────────────────────────────────────────────────────┐ │
│  │ Check: Is "auth" in session_state?                         │ │
│  └────────────────────────────────────────────────────────────┘ │
│                    ↓                         ↓                   │
│              [NO - New User]           [YES - Logged In]         │
│                    ↓                         ↓                   │
│  ┌──────────────────────────┐   ┌──────────────────────────┐   │
│  │ Google OAuth2 Flow       │   │ Verify User Still        │   │
│  │ - Show "Continue with    │   │ Authorized               │   │
│  │   Google" button         │   │ - Check against          │   │
│  │ - User clicks & logs in  │   │   allowed_users list     │   │
│  │ - Decode JWT token       │   │ - Show welcome message   │   │
│  │ - Extract email          │   └──────────────────────────┘   │
│  │ - Check against          │                 ↓                 │
│  │   allowed_users list     │         [PROCEED TO MAIN APP]    │
│  └──────────────────────────┘                                   │
│            ↓            ↓                                        │
│      [AUTHORIZED]  [DENIED]                                     │
│            ↓            ↓                                        │
│    Save to session  Show error & stop                           │
│    Rerun app                                                     │
└─────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────┐
│                   DATA INITIALIZATION                            │
│  ┌────────────────────────────────────────────────────────────┐ │
│  │ Check: Is refresh_page = True?                             │ │
│  │   YES → Clear cache                                        │ │
│  │   NO → Use cached data                                     │ │
│  └────────────────────────────────────────────────────────────┘ │
│                              ↓                                   │
│  ┌────────────────────────────────────────────────────────────┐ │
│  │ Execute snf_queries() - Pull from Snowflake:               │ │
│  │                                                             │ │
│  │ 1. BACKLOG_DF - Inbound SMS messages                       │ │
│  │    • SMS from last 2 weeks                                 │ │
│  │    • Case status = 'New'                                   │ │
│  │    • Member details, phone, message body                   │ │
│  │    • Touchpoint info, language, timestamps                 │ │
│  │                                                             │ │
│  │ 2. HISTORY_DF - Full touchpoint history per member         │ │
│  │    • All past touchpoints for members in queue             │ │
│  │    • Touchpoint type, message, outcome codes               │ │
│  │                                                             │ │
│  │ 3. SUBCODE_DF - Valid outcome codes & subcodes             │ │
│  │    • For SMS modality only                                 │ │
│  │    • Frequency counts                                      │ │
│  │                                                             │ │
│  │ 4. LANG_DF - Language reference list                       │ │
│  │    • From languages(in).csv                                │ │
│  └────────────────────────────────────────────────────────────┘ │
│                              ↓                                   │
│  ┌────────────────────────────────────────────────────────────┐ │
│  │ Initialize Session State Variables:                        │ │
│  │  • backlog_df, history_df, subcode_df, lang_df            │ │
│  │  • Filter variables (client, program, language, date)     │ │
│  │  • UI state (start_button, sms_idx, next_step)            │ │
│  │  • Case management (outcome_code, response_touse, etc.)   │ │
│  └────────────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────┐
│                      MAIN UI LAYOUT                              │
│  ┌────────────────────────────────────────────────────────────┐ │
│  │ Display:                                                    │ │
│  │  • SameSky Health Logo                                     │ │
│  │  • Title: "Inbound SMS Case Queue"                         │ │
│  │  • Header: "SMS Backlog"                                   │ │
│  └────────────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────┐
│                    FILTER SECTION (5 columns)                    │
│  ┌──────────┬──────────┬──────────┬──────────┬──────────────┐  │
│  │ Client   │ Program  │Touchpoint│ Language │  Date Range  │  │
│  │ Filter   │ Filter   │  Filter  │  Filter  │   Filter     │  │
│  │          │          │          │          │              │  │
│  │ ['All' + │ ['All' + │ ['All' + │ ['All' + │ Last Day /   │  │
│  │  list]   │  list]   │  list]   │  list]   │ Week / 2Wks  │  │
│  └──────────┴──────────┴──────────┴──────────┴──────────────┘  │
│                              ↓                                   │
│  ┌────────────────────────────────────────────────────────────┐ │
│  │ Dynamic Filtering Logic:                                   │ │
│  │  • Program filter updates based on Client selection        │ │
│  │  • Touchpoint filter updates based on Client & Program     │ │
│  │  • Any filter change resets start_checkbox to False        │ │
│  │  • Apply all filters to backlog_df → df_toshow            │ │
│  └────────────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────┐
│                   BACKLOG TABLE DISPLAY                          │
│  ┌────────────────────────────────────────────────────────────┐ │
│  │ Show filtered queue (df_toshow):                           │ │
│  │  Columns: STATUS, CHG_RESPONSE, CLIENT, PROGRAM,           │ │
│  │           TOUCHPOINT_NAME, BODY, MESSAGE_SENT,             │ │
│  │           CREATED_DATE, OUTCOME_CODE, OUTCOME_SUBCODE      │ │
│  └────────────────────────────────────────────────────────────┘ │
│                              ↓                                   │
│  ┌────────────────────────────────────────────────────────────┐ │
│  │ Checkbox: "Check to start texting"                         │ │
│  │   • When unchecked: Reset sms_idx to 0                     │ │
│  │   • When checked: Start processing queue                   │ │
│  └────────────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────────┘
                              ↓
                    [Start Button Checked?]
                              ↓
                        [YES] ↓ [NO - Stop]
                              ↓
┌─────────────────────────────────────────────────────────────────┐
│                   MESSAGE PROCESSING LOOP                        │
│  ┌────────────────────────────────────────────────────────────┐ │
│  │ Check if all messages processed (all_done = True)          │ │
│  │   YES → Show completion message & balloons                 │ │
│  │   NO → Continue to current message                         │ │
│  └────────────────────────────────────────────────────────────┘ │
│                              ↓                                   │
│  ┌────────────────────────────────────────────────────────────┐ │
│  │ Skip to first unprocessed message:                         │ │
│  │  • Check if current message already has STATUS             │ │
│  │  • If yes, find first message with STATUS = None           │ │
│  │  • Update sms_idx to that position                         │ │
│  └────────────────────────────────────────────────────────────┘ │
│                              ↓                                   │
│  ┌────────────────────────────────────────────────────────────┐ │
│  │ Get Current Message: tmp_df = df_toshow.iloc[sms_idx]     │ │
│  └────────────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────┐
│              CURRENT MESSAGE DISPLAY (3 columns)                 │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │ COLUMN 1 (30%) - Member Details:                         │  │
│  │  • Member Name (First + Last)                            │  │
│  │  • Member Age (calculated from DOB)                      │  │
│  │  • **Inbound Message Body**                              │  │
│  │  • Date Sent                                             │  │
│  │  • Client & Program                                      │  │
│  │  • Block Name & Touchpoint Name                          │  │
│  │  • Last Outbound Message Sent                            │  │
│  └──────────────────────────────────────────────────────────┘  │
│                                                                  │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │ COLUMN 2 (50%) - Touchpoint History:                     │  │
│  │  • Table showing last 10 touchpoints for this member     │  │
│  │  • Columns: Date, Type, Message                          │  │
│  │  • Filtered from history_df by member ID                 │  │
│  └──────────────────────────────────────────────────────────┘  │
│                                                                  │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │ COLUMN 3 (20%) - Action Menu:                            │  │
│  │  • Dropdown: "What do you want to do?"                   │  │
│  │    Options:                                              │  │
│  │      - None (default)                                    │  │
│  │      - Close Case w/ NO Response                         │  │
│  │      - Close Case & Respond                              │  │
│  │                                                           │  │
│  │  • Checkbox: "View escalations data"                     │  │
│  │    → Shows full member details for copy/paste            │  │
│  └──────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────┘
                              ↓
                  [Which Action Selected?]
                              ↓
        ┌─────────────────────┴─────────────────────┐
        ↓                                           ↓
┌───────────────────────────┐         ┌────────────────────────────┐
│  CLOSE CASE w/ NO RESPONSE│         │   CLOSE CASE & RESPOND      │
└───────────────────────────┘         └────────────────────────────┘
        ↓                                           ↓
┌───────────────────────────┐         ┌────────────────────────────┐
│ account_updates()         │         │ API Call: sms_api('list')  │
│                           │         │                            │
│ 1. Select Outcome Code    │         │ Get templated responses    │
│    (dropdown from         │         │ for this member            │
│     subcode_df)           │         │                            │
│                           │         │ Check for errors:          │
│ 2. Select Outcome Subcode │         │  • Status 422/404 = DNC    │
│    (dropdown filtered by  │         │  • Show warning            │
│     outcome code)         │         │  • Set api_error flag      │
│                           │         └────────────────────────────┘
│ 3. Special Cases:         │                     ↓
│    • If "Wrong Language"  │         ┌────────────────────────────┐
│      → Show language      │         │ Parse API Response:        │
│        dropdown           │         │  • Extract topics          │
│    • If "Wrong Number"    │         │  • Extract messages per    │
│      → Show phone input   │         │    topic                   │
│                           │         │  • Build response_df with: │
│ 4. Other Outcome Notes    │         │    - subjects              │
│    (text input)           │         │    - message_ids           │
│                           │         │    - message_name          │
│ 5. Positive Interaction   │         │    - message_source        │
│    Notes (text input)     │         └────────────────────────────┘
└───────────────────────────┘                     ↓
        ↓                           ┌────────────────────────────────┐
┌───────────────────────────┐       │ RESPONSE SELECTION (3 cols)   │
│ Validation:               │       │                                │
│  • Check outcome_subcode  │       │ COL 1: Response Selection      │
│    is not None            │       │  • Select Subject (dropdown)   │
│  • Show warning if missing│       │  • Select Message Name         │
└───────────────────────────┘       │  • Choose Message Text         │
        ↓                           │  • Show selected message       │
┌───────────────────────────┐       │  • Option to edit response     │
│ Button: "Close the case"  │       │    (text area)                 │
│  ↓                        │       │                                │
│ update_apptable()         │       │ COL 2: account_updates()       │
│  • Update STATUS          │       │  • Same as left path           │
│  • Update OUTCOME_CODE    │       │  • Outcome code/subcode        │
│  • Update OUTCOME_SUBCODE │       │  • Notes                       │
│  • Update CHG_RESPONSE    │       │                                │
│  • Set case_closed = True │       │ COL 3: Send Action             │
└───────────────────────────┘       │  • Show selected response      │
        ↓                           │  • Validation check            │
┌───────────────────────────┐       │  • Button: "Send & close case" │
│ case_account_api()        │       └────────────────────────────────┘
│                           │                     ↓
│ API Payload:              │       ┌────────────────────────────────┐
│  • phone                  │       │ Button Click Handler:          │
│  • modified_by (user)     │       │                                │
│  • case:                  │       │ 1. update_apptable()           │
│    - id                   │       │    • Update display status     │
│    - outcome_code         │       │                                │
│    - outcome_subcode      │       │ 2. sms_api('send')             │
│    - positive_notes       │       │    • Send message_id           │
│    - other_notes          │       │    • Include edited text       │
│  • account:               │       │      if modified               │
│    - id                   │       │    • Set case_closed = True    │
│    - language (if updated)│       │                                │
│                           │       │ 3. Show "Message Sent!"        │
│ Calls PROD API endpoint   │       │    • Set message_sent = True   │
│ Returns update status     │       │                                │
└───────────────────────────┘       │ 4. case_account_api()          │
        ↓                           │    • Close case in Salesforce  │
┌───────────────────────────┐       │    • Update account info       │
│ Show: "Case closed"       │       │                                │
└───────────────────────────┘       │ 5. Show "Account updated!"     │
        ↓                           └────────────────────────────────┘
┌───────────────────────────┐                     ↓
│ Button: "Go to next msg"  │       ┌────────────────────────────────┐
│  ↓                        │       │ Button: "Go to next message"   │
│ next_sms()                │       │  ↓                             │
└───────────────────────────┘       │ next_sms()                     │
                                    └────────────────────────────────┘
        └─────────────────────┬─────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────┐
│                      next_sms() Function                         │
│  ┌────────────────────────────────────────────────────────────┐ │
│  │ 1. Increment sms_idx by 1                                  │ │
│  │                                                             │ │
│  │ 2. Check if queue complete:                                │ │
│  │    • If sms_idx == len(df_toshow)                          │ │
│  │      → Set all_done = True                                 │ │
│  │    • Else: all_done = False                                │ │
│  │                                                             │ │
│  │ 3. Reset all UI state variables:                           │ │
│  │    • next_step = None                                      │ │
│  │    • outcome_code = None                                   │ │
│  │    • outcome_subcode = None                                │ │
│  │    • response_touse = None                                 │ │
│  │    • api_error = None                                      │ │
│  │    • case_closed = False                                   │ │
│  │    • message_sent = False                                  │ │
│  │    • All other flags reset                                 │ │
│  │                                                             │ │
│  │ 4. Trigger st.rerun() to refresh UI                        │ │
│  │                                                             │ │
│  │ 5. Loop back to MESSAGE PROCESSING LOOP                    │ │
│  └────────────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────────┘
                              ↓
                   [Continue until all_done = True]
                              ↓
┌─────────────────────────────────────────────────────────────────┐
│                       COMPLETION STATE                           │
│  ┌────────────────────────────────────────────────────────────┐ │
│  │ When all_done = True:                                      │ │
│  │  • Show: "You have responded to all text messages! ✓"     │ │
│  │  • Display balloons animation                             │ │
│  │  • User can adjust filters to load new queue              │ │
│  │  • Or uncheck "start texting" to reset                    │ │
│  └────────────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────┐
│                       LOGOUT SECTION                             │
│  ┌────────────────────────────────────────────────────────────┐ │
│  │ Always displayed at bottom:                                │ │
│  │  • Button: "Logout"                                        │ │
│  │                                                             │ │
│  │ On click:                                                  │ │
│  │  1. Clear st.cache_data                                    │ │
│  │  2. Delete session_state["auth"]                           │ │
│  │  3. Delete session_state["token"]                          │ │
│  │  4. Returns to OAuth login screen                          │ │
│  └────────────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────────┘
```

---

## 🔄 Key Functions Detail

### **snf_queries()**
```
Purpose: Fetch all data from Snowflake
├── Connect to Snowflake with credentials from secrets
├── Query 1: Backlog SMS (last 2 weeks, status='New')
│   └── Returns: backlog_df
├── Query 2: Member touchpoint history
│   └── Returns: history_df
├── Query 3: Outcome codes & subcodes
│   └── Returns: subcode_df
└── Load CSV: Language reference
    └── Returns: lang_df
```

### **sms_api(action, member_id, message_id, message_text)**
```
Purpose: Interact with SMS/Content API
├── action='list': Get available templated responses
│   ├── Input: member_id
│   └── Output: JSON with topics & messages
└── action='send': Send SMS response
    ├── Input: member_id, message_id, [optional] edited text
    └── Output: Send confirmation status
```

### **case_account_api()**
```
Purpose: Close case and update account in Salesforce
├── Build API payload:
│   ├── Case updates (outcome codes, notes)
│   └── Account updates (language, etc.)
├── POST to close_incoming_sms_case endpoint
└── Returns: Case closure & account update status
```

### **account_updates()**
```
Purpose: Collect outcome codes and notes from user
├── Display outcome code dropdown
├── Display outcome subcode dropdown (filtered)
├── Special handling:
│   ├── Wrong Language → Show language picker
│   └── Wrong Number → Show phone input
├── Collect "Other Outcome Notes"
└── Collect "Positive Interaction Notes"
```

### **update_apptable()**
```
Purpose: Update local dataframe with case status
├── Find current row index in backlog_df
├── Update STATUS column
├── Update OUTCOME_CODE column
├── Update OUTCOME_SUBCODE column
├── Update CHG_RESPONSE column
└── Set case_closed = True
```

### **next_sms()**
```
Purpose: Move to next message in queue
├── Increment sms_idx
├── Check if queue is complete
├── Reset all UI state variables
├── Reset action dropdowns
├── Reset outcome codes
└── Clear response selections
```

---

## 🔐 Security & Authentication

```
OAuth2 Flow (Google):
1. User clicks "Continue with Google"
2. Redirected to Google OAuth consent
3. User grants permission
4. Redirected back with token
5. JWT decoded to extract email
6. Email checked against allowed_users list
7. If authorized: Save to session_state
8. If denied: Show error & stop
```

---

## 💾 Session State Management

**Critical Session Variables:**
- `auth` - Logged in user email
- `backlog_df` - Full SMS queue
- `df_toshow` - Filtered queue
- `sms_idx` - Current message index
- `next_step` - User's selected action
- `outcome_code` / `outcome_subcode` - Case closure codes
- `response_touse` - Selected SMS response
- `case_closed` - Flag if case was closed
- `message_sent` - Flag if SMS was sent
- `all_done` - Flag if queue is complete

---

## 🌐 External API Integrations

### **1. SMS/Content API**
- **Endpoint:** `https://agd9kg004g.execute-api.us-west-2.amazonaws.com/v1/sender/sms_templated_responses`
- **Actions:**
  - `list`: Get available responses for member
  - `send`: Send templated or custom response

### **2. Case Close API**
- **Endpoint:** `https://agd9kg004g.execute-api.us-west-2.amazonaws.com/v1/sender/close_incoming_sms_case`
- **Purpose:** Update Salesforce case and account records

### **3. Snowflake Database**
- **Purpose:** Query SMS history, member data, touchpoint history
- **Connection:** snowflake-connector-python with credentials from secrets

### **4. Google OAuth2**
- **Purpose:** User authentication & authorization
- **Flow:** OAuth2Component with PKCE

---

## 📝 Logging

All actions are logged to: `sms_2_way_YYYY_MM_DD.log`

**Logged Events:**
- User navigation (next message)
- Outcome code selections
- API requests & responses
- Case closures
- Message sends

---

## 🎨 UI Layout Structure

```
┌──────────────────────────────────────────────────────┐
│ Logo                                                 │
│ Title: Inbound SMS Case Queue                        │
├──────────────────────────────────────────────────────┤
│ SMS Backlog Header                                   │
├──────────────────────────────────────────────────────┤
│ [Filter 1] [Filter 2] [Filter 3] [Filter 4] [Date]  │
├──────────────────────────────────────────────────────┤
│ [Filtered Queue Table]                               │
├──────────────────────────────────────────────────────┤
│ ☑ Check to start texting                            │
├──────────────────────────────────────────────────────┤
│ Texting Header                                       │
├──────────────────────────────────────────────────────┤
│ [Member Details] [History] [Action Menu]             │
│ [30% width]      [50%]     [20%]                     │
├──────────────────────────────────────────────────────┤
│ [Dynamic content based on action selected]           │
│ - No Response workflow                               │
│ - Send Response workflow                             │
├──────────────────────────────────────────────────────┤
│ Logout Section                                       │
│ [Logout Button]                                      │
└──────────────────────────────────────────────────────┘
```

---

## ⚙️ Error Handling

- **DNC/DNT Members:** API returns 422/404 → Show warning, prevent sending
- **Missing Subcode:** Validation prevents case closure
- **Snowflake Connection:** insecure_mode=True to bypass OCSP issues
- **OAuth Failures:** User sees error and cannot proceed
- **API Failures:** Logged and displayed to user

---

## 🔄 State Reset Triggers

**Queue resets when:**
- User unchecks "start texting" checkbox
- Filters are changed
- User logs out

**Message state resets when:**
- Moving to next message
- Case is closed
- Response is sent

---

This flowchart represents the complete application logic as of December 11, 2025.
